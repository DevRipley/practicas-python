def calcular_area(base, altura):
    area = 0.5
    area_triangulo = area * base * altura
    return print(f"Area de el triangulo: {area_triangulo}")

# base = float(input("Base: "))
# altura = float(input("Altura: "))

# calcular_area(base, altura)